{
  "name": "pyo-insitute-ui",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "postcss style.css -o src/style.css",
    "dev": "npm run build && live-server src --port=6400",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^10.4.7",
    "node-sass": "^7.0.1",
    "postcss-cli": "^9.1.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.1.4"
  }
}
